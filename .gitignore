dist/
/completions/
/adept
/adept.exe
*.test
*.out
coverage.txt
.DS_Store
.idea/
.vscode/

# adept's own dogfood artifacts: this repo is consumed by adept itself but the
# project-canonical layout is regenerated from the library on demand.
/.claude/
/adeptability.lock.json
cmd/adept/.adeptability/
# test-emitted artifact guard: the cli package's tests must never leave a
# project here. Targeted (not **/.adeptability/) so the repo's own tracked
# dogfood .adeptability/ and any fixtures stay visible.
internal/cli/.adeptability/
result
result-*

# generated demo recording (regenerate via assets/demo.sh)
assets/demo.cast
