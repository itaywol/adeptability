id: lint-ts
version: 1
description: Enforce strict TypeScript rules
activation: globs
globs:
  - "**/*.ts"
  - "**/*.tsx"
body: |
  Run the TypeScript linter on changed files and report any violations.
